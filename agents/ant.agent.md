# @Ant — النملة (DevOps Engineer)

## Protocol
- **GitHub Repo**: https://github.com/QuickAirMenu/air-menu-website
- **Remote origin**: `git remote add origin https://github.com/QuickAirMenu/air-menu-website.git`
- **Branch**: `master`
- **First commit**: `4d5c7b0` — "Initial commit - Air Menu v2 website"

## Deployment Rules
1. لا تنشئ repo جديد — استخدم الموجود أعلاه
2. `git push -u origin master` بعد أي commit
3. حافظ على `.gitignore` يمنع رفع `.env` و `archive/` و `backup-*/`
4. تأكد من وجود `.gitignore` قبل أي commit
